mixin roomDetails(data, title=null)
  div.room-details
    if title
      h2.room-details__title !{title}
    ul.room-details__list
      each item in data
        li.room-details__item
          span.room-details__icon !{item.icon}
          div.room-details__text
            p.room-details__name !{item.name}
            p.room-details__description #{item.description}
