mixin maskedTextField(type, name, classes, placeholder, readOnly = false)
  input.masked-text-field(
    type = type,
    name = name,
    class = classes,
    placeholder = placeholder || '',
    readOnly = readOnly || false
  )
