mixin textField(type, name, classes, id, placeholder, readOnly = false)
  -
    let classesList
    if(typeof(classes) !== "undefined" && classes) {
    classesList = classes.split(",")
    }
    let allClasses = "";
    if(typeof(classesList) !== "undefined" && classesList) {
      for (let i = 0; i < classesList.length; i++) {
        allClasses = allClasses + " text-field_" + classesList[i].trim()
      }
    }
  input.text-field(
    type = type,
    name = name,
    class = allClasses,
    id = id || '',
    placeholder = placeholder || '',
    readOnly = readOnly || false
  )
